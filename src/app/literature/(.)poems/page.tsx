import Card from '@/components/card/card';

const Poems = () => {
  return (
    <Card>
      Intercepted Poems (smooth routing). Refresh and see the difference
    </Card>
  );
};

export default Poems;
